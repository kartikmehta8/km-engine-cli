module.exports = async function help() {
  console.log(`
    km list      - List all templates with descriptions
    km add       - Add a new template
    km delete    - Delete a template by name
    km use       - Use a template by name
    km help      - Show help menu
  `);
};
